<UserControl
    x:Class="AIImageGuide.Views.AdminPanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:AIImageGuide.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock
            Margin="0,0,0,10"
            FontSize="24"
            FontWeight="Bold"
            Text="Admin Panel - User Management" />
        <DataGrid
            x:Name="UsersDataGrid"
            Grid.Row="1"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            ItemsSource="{Binding}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Binding="{Binding Id}"
                    Header="ID"
                    IsReadOnly="True" />
                <DataGridTextColumn
                    Binding="{Binding Username}"
                    Header="Username"
                    IsReadOnly="True" />
                <DataGridTextColumn
                    Binding="{Binding Email}"
                    Header="Email"
                    IsReadOnly="True" />
                <DataGridTextColumn
                    Binding="{Binding Role}"
                    Header="Role"
                    IsReadOnly="True" />
                <DataGridCheckBoxColumn
                    Binding="{Binding IsBlocked}"
                    Header="Blocked"
                    IsReadOnly="True" />
                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button
                                    Margin="5,0"
                                    Click="BlockButton_Click"
                                    Content="{Binding IsBlocked, Converter={StaticResource BlockButtonConverter}}" />
                                <Button
                                    Margin="5,0"
                                    Click="ToggleRoleButton_Click"
                                    Content="Toggle Role" />
                                <Button
                                    Margin="5,0"
                                    Click="DeleteButton_Click"
                                    Content="Delete" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel
            Grid.Row="2"
            Margin="0,10,0,0"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button
                x:Name="PreviousButton"
                Margin="5"
                Padding="10,5"
                Click="PreviousButton_Click"
                Content="Previous"
                IsEnabled="False" />
            <TextBlock
                x:Name="PageInfoTextBlock"
                Margin="5"
                VerticalAlignment="Center" />
            <Button
                x:Name="NextButton"
                Margin="5"
                Padding="10,5"
                Click="NextButton_Click"
                Content="Next" />
        </StackPanel>
    </Grid>
</UserControl>
